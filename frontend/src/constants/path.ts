const path = {
  home: '/',
  login: '/login',
  register: '/register',
  verifyRegister: '/verify-register',
  profile: '/profile',
  reservation: '/reservation',
  dashboard: '/admin/dashboard'
} as const
export default path
